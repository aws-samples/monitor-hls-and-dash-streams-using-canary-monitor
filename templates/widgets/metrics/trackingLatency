{
  "x": {{positions[0]}},
  "y": {{positions[1]}},
  "height": {{dashboardconfig['metric']['height']}},
  "width": {{dashboardconfig['metric']['width']}},
  "type": "metric",
  "properties": {
    "metrics": [
      {% set ns = namespace(count = 0) %}
      {% for endpoint in renderinfo['endpoints'] %}
        {% if endpoint['config']['tracking']['get'] %}
          {% if ns.count > 0 %}, {% endif %}[ "CanaryMonitor", "Latency", "Origin", "{{renderinfo['origin']}}", "Type", "{{renderinfo['type']}}", "Endpoint", "{{endpoint['endpoint']}}", "Technology", "{{endpoint['technology']}}", "RequestType", "tracking", "Workload", "{{renderinfo['workload']}}" ]{% set ns.count = ns.count + 1 %}
        {% endif %}
      {% endfor %}
    ],
    "view": "timeSeries",
    "stacked": false,
    "region": "{{dashboardconfig['region']}}",
    "stat": "Average",
    "period": 60,
    "title": "Tracking Latency [msec] (avg)",
    "yAxis": {
      "left": {
        "showUnits": false,
        "label": "",
        "min": 0
      },
      "right": {
        "showUnits": false
      }
    },
    "legend": {
      "position": "bottom"
    },
    "annotations": {
      "horizontal": [
        {
          "label": "Threshold",
          "value": 1000,
          "fill": "above"
        }
      ]
    },
    "liveData": true
  }
}